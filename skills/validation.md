# Validation

Purpose: decide whether the result satisfies the task.

Rules:
- Compare output against the original request and spec.
- Check tests, artifacts, and visible UI state.
- Require another pass when acceptance criteria fail.

Expected output: pass/fail validation with evidence.
